name: schema_valid
description: Dart model validation with annotations and code generation
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.11.0

dev_dependencies:
  build_runner: ^2.4.0
  source_gen: ^1.5.0
  analyzer: